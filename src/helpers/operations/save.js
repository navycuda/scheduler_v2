// src/helpers/operations/save.js
// @ts-check
/**
 * 
 * @param {String} name The student name 
 * @param {Interviewer} interviewer The selected interviewer
 */
const save = (name, interviewer) => {
  const interview = {
    student: name,
    interviewer
  };
};

export default save;